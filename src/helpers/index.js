// ==========helpers function========
const response = require("./response");
const sendEmail = require("./mailer");
// ==========generate obj============
const helpers = {
  response,
  sendEmail
};

// ==========export helpers==========
module.exports = helpers;
